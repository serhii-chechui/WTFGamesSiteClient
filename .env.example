# Copy this file to .env (gitignored) to point the app at a different API.
# If unset, the app falls back to the current production API
# (see src/api/client.js).
REACT_APP_API_URL=https://api.wtfgames.com.ua/api
