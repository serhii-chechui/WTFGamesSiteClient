# Copy to .env.development.local to override the local development API.
# Production uses .env.production; override via CI or .env.production.local.
# This value is public and embedded into the browser bundle.
REACT_APP_API_URL=http://127.0.0.1:3156/api
